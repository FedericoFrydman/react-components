import { ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import { XUser } from './XUser';

# Cross Domain User

XUser (Cross Domain User) renders information about a user signed into another realm/domain.

<ArgsTable of={XUser} />